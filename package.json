{
  "devDependencies": {
    "concurrently": "^9.2.0"
  },
 "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"cd Backend && npm run dev\" \"cd Frontend && npm run dev\"",
    "serve": "concurrently \"cd Backend && npx nodemon index.js\" \"cd Frontend && npm run dev\""
  }
}
